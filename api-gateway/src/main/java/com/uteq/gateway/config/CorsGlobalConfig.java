package com.uteq.gateway.config;

/**
 * CORS global ahora definido únicamente en application.yml (spring.cloud.gateway.globalcors).
 * Clase vacía para eliminar configuración duplicada previa.
 */
public class CorsGlobalConfig {
    // Intencionalmente vacío
}
